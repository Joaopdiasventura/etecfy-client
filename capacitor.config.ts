import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.etecfy.joaopdias',
  appName: 'etecfy',
  webDir: 'dist/etecfy/browser',
};

export default config;
